<%= form_with(model: alternative, local: true) do |form| %>
  <% if alternative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternative.errors.count, "error") %> prohibited this alternative from being saved:</h2>

      <ul>
      <% alternative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :alternative_name %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight, id: :alternative_weight %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :alternative_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
